import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Lessons = () => {
  return (
    <View>
      <Text>Lesson.component</Text>
    </View>
  );
};

export default Lessons;

const styles = StyleSheet.create({});
